<?xml version="1.0" encoding="utf-8"?>
<pysystest type="auto">

    <description>
        <title>OBXCoin - check for contract deployment using json abi</title>
        <purpose><![CDATA[]]>
            Uses one web3 instance for account1 to compiled and deploy the OBX ERC20 contract and transfer
            balance to account2. Then creates another web3 instance using the contract address and the json
            abi to call functions against the contract. This simulates creating a contract instance from the
            contract address only and an external json abi.
        </purpose>
    </description>

    <classification>
        <groups inherit="true">
            <group></group>
        </groups>
        <modes inherit="true">
            <mode>obscuro</mode>
            <mode>ropsten</mode>
            <mode>ganache</mode>
        </modes>
    </classification>

    <data>
        <class name="PySysTest" module="run"/>
    </data>

    <traceability>
        <requirements>
            <requirement id=""/>
        </requirements>
    </traceability>
</pysystest>
